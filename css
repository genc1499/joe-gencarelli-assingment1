// Blog page styles

// Display header imahe
.blog-header{
    background-image: url("../assets/blog-header-1.jpg");
    background-size:cover;
    background-position:center;
    background-repeat:no-repeat;
    min-height:65vh;
    width:100vw;
}
    // Image the overlaps into header
    .new-menu-image{
    position:absolute;
    right:0;
    top:370px;
    width:63%;
    z-index:1;
        img{
            box-shadow:-13px 19px 0px -2px $tertiary;
        }
    }


// Add backlground-color
.blog-post-text{
    background-color:$quaternary;
    padding:60px 0 60px 0;
    
    .wrapper{
        width:90%;
        max-width:1200px;
        margin:0 auto;
    }

 

    // Re-size imgage
    .mission-image{
        width:25%;
        margin-top:5%;
        position:absolute;
        left:0;
        top:40px;
        img{
            box-shadow:38px 4px 0px -5px $quinary;
        }
    }
    
    // Flex container container containing mission and recent posts
    .mission{
        display:flex;
        flex-direction:column; 
        width:50%;
    }
  
    // Aside styles
    aside{
        display:flex;
        flex-direction:column;
        justify-content: space-between;
        width:68%;
     
        padding: 15px 19px 13px 21px;
        position:relative;
        
    
        h3{
            font-size:1.95rem;
            margin-bottom:0;
        }

        p{
            font-size:1.4rem;
            line-height:19px;
            margin:-10px 0 0 0;
        }
        .mission-text{
            margin-top:7px;
        }
            a{
            color:$tertiary;
            }
            li{
            line-height:15px;
            margin-left:20px;
            position:relative;
            font-size:1.4rem;
            line-height:19px;
            letter-spacing: 1.9px;
            font-family:$bodyFont;
               
            // Add coloured bullets to each list item
            &::before{
                content:"•";
                position: absolute;
                color: $secondary;
                font-size: 4rem;
                left: -20px;
                display:inline-block;
            }
        }
    }
    
    .blog-container{
        display:flex;
        margin-top:27%;
        position:relative;
    }
    
    article{
        padding:20px 1px 41px 73px;
        position:relative;
        border-left:5px solid $tertiary;
        // margin-left:12%;
        width:96%;
        .blog-date{
            margin-top:-10px;
            color:$tertiary;
        }

        // Add top and bottom horizontal border that extends outside wrapper contraints

        &::before{
            content:'';
            position:absolute;
            border-top:5px solid $tertiary;
            top: 0px;
            width: 115%;
            left: 0px;
        }

        &::after{
            content:'';
            position:absolute;
            border-bottom:5px solid $tertiary;
            bottom: 0px;
            width: 115%;
            left: 0px; 
        }
        
    }
    // Re-size elements in blog post
    .new-menu-blog-post{
        h3{
            font-size:1.95rem;
        }

        p{
            font-size:1.4rem;
            line-height:19px;
        }

 
    }   
}

// Comments Section
.comments-container{
    background-color:$quaternary;
    padding-bottom:25px;

    // Align h2 to be flush with each comment section
    h4{
        align-self:center;
        width:20%;
        font-size:1.6rem;
    }

    p{
        font-size:1.4rem;
        line-height:19px;
        margin:0 0 0 0;
    }

    // Style the dates of each comment
    .commnet-date{
        color:$tertiary;
        margin-bottom:10px;
    }

    // Flex so each comment and the form are stacked on top of eachother
    .comments{
        display:flex;
        flex-direction:column;
        align-items:flex-end;
        margin-top:-53px;

        .comment-text{
            width:80%;
            margin-left:50px;
        }
        
         // Add some apce between each comment section
        .top-comment{
            margin-bottom:100px;
        }

        // Flex each comment container so image and text are-side-by side
        .top-comment, .second-comment{
            display:flex;
            width:60%;
        }

        // Adjusted Form Stylings
        // Remove box shadow and increase width
        form{
            box-shadow:none;
            width:65%;
            padding-left:70px;
            padding-right:0px;   
        }
       

        // Change background color from inputs and form
        // Add border-bottom to input fields
        form{
            background-color: revert;
            input{
                background-color: $quaternary;
                border-bottom:2px solid $tertiary;
            }
            textarea{
                background-color:$quaternary;
                border:2px solid $tertiary;
            }
        }
     
    }
}

